# Fully pinned so CI installs exactly what production runs.
# Regenerate with: pip install -U <pkg> && pip list --format=freeze
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.5.0
blinker==1.9.0
python-dotenv==1.2.3

# Postgres driver — unused when POSTGRES_URL is absent (Lightsail uses SQLite).
psycopg2-binary==2.9.12

# WSGI servers
gunicorn==26.2.0 ; sys_platform != "win32"
waitress==3.0.2 ; sys_platform == "win32"
